<form method="post" action="{{ path('article_delete', {'id': article.id}) }}" onsubmit="return confirm('vous etes sur le point de supprimer ce fichier');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="btn">Delete</button>
</form>
