{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ article.name }}</td>
            </tr>
            <tr>
                <th>Contents</th>
                <td>{{ article.contents }}</td>
            </tr>
            <tr>
                <th>image</th>
                <td><img src="{{vich_uploader_asset(article, 'imageFile')}}" alt="card-img-top" style="width: 30Opx; height: 300px; "></td>

            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_index') }}">retour a la liste</a>

    <a href="{{ path('article_edit', {'id': article.id}) }}"> mettre a jour</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
