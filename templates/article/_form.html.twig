{{ form_start(form) }}
    {{ form_widget(form) }}
    {# verifier si la route est bien article_edit pour afficher les images #}
    {% if app.request.attributes.get('_route') == 'article_edit' %}
    {% for pictures in article.picture %}
    <h2>plein visu images</h2>
    <div>
    <img src = "{{asset('/public/uploads/articleImages/' ~ picture.name) }}" alt="image" width="150">

        {# On ajoute un lien permettant de supprimer une image (sera géré en Ajax) #}
        <a href"{{path('article_delete_picture', {id: picture.id})}}" 
        data-delete data-token="{{ csrf_token('delete' ~ picture.id )}}">Supprimer</a>
    </div>
        {% endfor %}
    {% endif %}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
