{% extends 'base.html.twig' %}

{% block title %}RefCritere{% endblock %}

{% block body %}
    <h1>RefCritere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ref_critere.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ref_critere.name }}</td>
            </tr>
            <tr>
                <th>Ponderation</th>
                <td>{{ ref_critere.ponderation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ref_critere_index') }}">back to list</a>

    <a href="{{ path('ref_critere_edit', {'id': ref_critere.id}) }}">edit</a>

    {{ include('ref_critere/_delete_form.html.twig') }}
{% endblock %}
