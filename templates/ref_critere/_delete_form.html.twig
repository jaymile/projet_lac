<form method="post" action="{{ path('ref_critere_delete', {'id': ref_critere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ref_critere.id) }}">
    <button class="btn">Delete</button>
</form>
