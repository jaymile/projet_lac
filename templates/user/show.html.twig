{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>{{ user.firstname }} {{ user.lastname }} </h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Picture</th>
                <td>{{ user.picture }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ user.sexe }}</td>
            </tr>
           
        </tbody>
    </table>

<div class="container">
    
    <h3>liste de vos articles</3>
    {% for article in app.user.articles %}

    <div>
    <h4> ARTICLE</h4>
   <a href="{{ path('article_show', {'id': article.id}) }}">{{article.name}}</a>
    </div>

    {% endfor %}
</div>


<div class="container">
    
    <h3>liste de vos hebergements</3>
    {% for lodging in app.user.lodgings %}

    <div>
    <h4> hebergements</h4>
    <a href="{{ path('lodging_show', {'id': lodging.id}) }}">{{lodging.name}}</a>
    
    </div>

    {% endfor %}
</div>

    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
